# 六人掼蛋移动端游戏实现方案

## 1. 项目概述

### 1.1 项目目标

开发一款同时支持 **iOS** 和 **Android** 的六人在线掼蛋手机游戏。

核心功能包括：

- 用户登录
- 创建房间
- 通过房间号加入房间
- 六名玩家进入同一房间
- 玩家准备
- 服务端发牌
- 按掼蛋规则进行游戏
- 玩家出牌、过牌
- 游戏状态实时同步
- 一局结束后进行结算
- 支持再来一局
- 支持玩家掉线后重新连接
- Linux 云服务器提供网络和游戏服务

---

# 2. 总体技术架构

推荐采用：

- **客户端：Flutter + Dart**
- **服务端：C++20**
- **网络通信：HTTPS + WebSocket Secure（WSS）**
- **网络框架：Boost.Asio + Boost.Beast**
- **数据库：PostgreSQL**
- **缓存：Redis（第二阶段引入）**
- **反向代理：Nginx**
- **部署环境：Linux 云服务器**

整体架构：

```text
                Android
                   │
                   │
                   ▼
             Flutter Client
                   │
                   │ HTTPS / WSS
                   ▼
                Nginx
                   │
                   ▼
          C++ Game Server
       ┌───────────┼────────────┐
       │           │            │
    Session     RoomManager  GameEngine
       │           │            │
       └───────────┼────────────┘
                   │
          ┌────────┴────────┐
          │                 │
      PostgreSQL          Redis
```

iOS 与 Android 共用一套 Flutter 业务代码。

---

# 3. 为什么使用 WebSocket

掼蛋属于低频实时棋牌游戏。

与 FPS 游戏相比，它不需要每秒几十次的位置同步，而主要传输以下事件：

```text
进入房间
准备
开始游戏
发牌
出牌
过牌
轮到下一玩家
结算
聊天
心跳
断线重连
```

因此不建议第一版自行维护 TCP + UDP 双通道。

推荐：

```text
HTTPS
+
WebSocket Secure（WSS）
```

## HTTPS 负责

- 登录
- 注册
- 用户信息
- 战绩查询
- 房间历史
- 头像等非实时业务

## WebSocket 负责

- 创建房间
- 加入房间
- 玩家准备
- 开始游戏
- 出牌
- 过牌
- 游戏状态广播
- 心跳
- 掉线检测
- 断线重连
- 游戏结算

---

# 4. 客户端方案

## 4.1 技术选择

```text
Flutter
Dart
Riverpod / Provider
HTTP
WebSocket
JSON
```

Flutter 可以使用一套主要代码同时维护 iOS 和 Android 客户端。

---

## 4.2 推荐目录结构

```text
lib/

├── pages/
│   ├── login/
│   ├── lobby/
│   ├── room/
│   ├── game/
│   └── profile/
│
├── widgets/
│   ├── poker_card.dart
│   ├── player_widget.dart
│   ├── game_table.dart
│   └── room_player.dart
│
├── network/
│   ├── http_client.dart
│   ├── websocket_client.dart
│   ├── protocol.dart
│   └── reconnect_manager.dart
│
├── models/
│   ├── user.dart
│   ├── room.dart
│   ├── player.dart
│   ├── card.dart
│   └── game_state.dart
│
├── game/
│   ├── game_controller.dart
│   ├── card_controller.dart
│   └── animation_manager.dart
│
└── main.dart
```

---

# 5. 用户基本操作流程

```text
启动 App
   │
   ▼
登录
   │
   ▼
游戏大厅
   │
   ├───────────────┐
   │               │
   ▼               ▼
创建房间        输入房间号
   │               │
   └───────┬───────┘
           ▼
        房间页面
           │
           ▼
      六名玩家进入
           │
           ▼
        全部准备
           │
           ▼
        开始游戏
           │
           ▼
         发牌
           │
           ▼
       轮流出牌
           │
           ▼
       本局结束
           │
           ▼
         结算
           │
      ┌────┴────┐
      ▼         ▼
   再来一局    退出
```

---

# 6. 服务端方案

## 6.1 推荐技术栈

```text
C++20

Boost.Asio
Boost.Beast

CMake

PostgreSQL

Redis

Nginx

Linux
```

---

## 6.2 推荐服务端目录

```text
server/

├── main.cpp
│
├── network/
│   ├── websocket_server.cpp
│   ├── websocket_session.cpp
│   ├── connection_manager.cpp
│   └── protocol.cpp
│
├── auth/
│   ├── auth_service.cpp
│   └── token_manager.cpp
│
├── user/
│   ├── user.cpp
│   └── user_manager.cpp
│
├── room/
│   ├── room.cpp
│   ├── room_manager.cpp
│   └── room_task_queue.cpp
│
├── game/
│   ├── game_engine.cpp
│   ├── game_state.cpp
│   ├── card.cpp
│   ├── deck.cpp
│   ├── hand.cpp
│   ├── card_analyzer.cpp
│   ├── rule_engine.cpp
│   └── settlement.cpp
│
├── database/
│   ├── postgresql.cpp
│   └── redis.cpp
│
├── protocol/
│   ├── message.cpp
│   ├── message_type.cpp
│   └── error_code.cpp
│
├── service/
│   ├── room_service.cpp
│   └── game_service.cpp
│
└── utils/
    ├── logger.cpp
    ├── timer.cpp
    ├── uuid.cpp
    └── random.cpp
```

---

# 7. 核心原则：服务器权威

游戏中的所有关键状态必须由服务器决定。

客户端不能自己决定：

- 是否轮到当前玩家
- 玩家有哪些牌
- 出牌是否合法
- 牌型是什么
- 是否能够压过上一手牌
- 下一位玩家是谁
- 本局是否结束
- 谁获得胜利
- 得分、升级和结算结果

客户端只负责：

```text
输入
+
显示
+
动画
```

服务器负责：

```text
规则
+
状态
+
判定
+
结果
```

---

## 7.1 正确的出牌流程

玩家选择：

```text
3♠
3♥
3♦
```

客户端发送：

```json
{
  "type": "play_cards",
  "request_id": 123456,
  "room_id": "938271",
  "cards": [12, 13, 14]
}
```

服务器：

```text
检查玩家身份
    ↓
检查是否在该房间
    ↓
检查房间当前是否处于 PLAYING
    ↓
检查是否轮到该玩家
    ↓
检查这些牌是否属于该玩家
    ↓
分析牌型
    ↓
检查牌型是否合法
    ↓
判断能否压过上一手
    ↓
更新 GameState
    ↓
生成新的 state_version
    ↓
广播给房间中的所有玩家
```

---

# 8. GameEngine 游戏规则引擎

建议将整个掼蛋游戏逻辑从网络系统中完全独立。

```text
GameEngine
   │
   ├── Card
   ├── Deck
   ├── Hand
   ├── CardAnalyzer
   ├── CardPattern
   ├── RuleEngine
   ├── GameState
   ├── TurnManager
   └── Settlement
```

例如：

```cpp
class GuandanEngine {
public:
    void startGame();

    PlayResult playCards(
        PlayerId player,
        const std::vector<CardId>& cards
    );

    PlayResult pass(PlayerId player);

    bool isGameOver() const;

    const GameState& getState() const;
};
```

这样 GameEngine 可以脱离网络单独进行自动化测试。

---

# 9. Card 设计

不要直接用字符串作为服务器内部牌数据。

建议：

```cpp
enum class Suit {
    SPADE,
    HEART,
    CLUB,
    DIAMOND,
    JOKER
};

enum class Rank {
    R2,
    R3,
    R4,
    R5,
    R6,
    R7,
    R8,
    R9,
    R10,
    J,
    Q,
    K,
    A,
    SMALL_JOKER,
    BIG_JOKER
};

struct Card {
    uint16_t id;
    Suit suit;
    Rank rank;
};
```

每张实体牌具有唯一 `CardId`。

如果使用多副牌，即使有两张完全相同的红桃 3，也应该具有不同 CardId。

---

# 10. 牌型识别系统

可以定义：

```cpp
enum class CardType {
    INVALID,

    SINGLE,

    PAIR,

    TRIPLE,

    TRIPLE_WITH_PAIR,

    STRAIGHT,

    THREE_PAIRS,

    TWO_TRIPLES,

    STRAIGHT_FLUSH,

    BOMB,

    JOKER_BOMB
};
```

具体牌型应以最终确定的六人玩法规则为准。

牌型分析器：

```cpp
class CardAnalyzer {
public:
    CardPattern analyze(
        const std::vector<Card>& cards,
        const RuleContext& context
    );
};
```

例如：

```text
输入：

3♠ 3♥

输出：

type = PAIR
rank = 3
```

---

# 11. RuleEngine

RuleEngine 专门负责规则比较。

```cpp
class RuleEngine {
public:
    bool canBeat(
        const CardPattern& current,
        const CardPattern& previous,
        const RuleContext& context
    );
};
```

负责处理：

- 单牌比较
- 对子比较
- 三张比较
- 顺子
- 三连对
- 钢板
- 同花顺
- 炸弹
- 王炸
- 级牌
- 逢人配
- 不同张数炸弹的优先级

不要将这些规则散落在网络代码或 UI 中。

---

# 12. 六人掼蛋规则问题

这是整个项目开发前必须最先确定的问题。

经典掼蛋通常为四人玩法。

如果要做六人版本，则必须明确你采用的是：

- 已存在的某种六人地区规则；
- 还是基于传统四人规则自行扩展的六人玩法。

一种可能的组队方法：

```text
座位：

           P3

      P2        P4

      P1        P5

           P0
```

队伍：

```text
A队：

P0
P2
P4

B队：

P1
P3
P5
```

即：

```text
A B A B A B
```

但是开发前必须形成正式规则文档。

至少需要确认：

1. 使用几副牌？
2. 总共多少张牌？
3. 六人如何平均发牌？
4. 是否存在底牌？
5. 三名队友如何结算？
6. 头游、二游、三游如何定义？
7. 剩余玩家的名次如何处理？
8. 如何升级？
9. 是否有进贡？
10. 如何确定进贡对象？
11. 如何还贡？
12. 是否支持抗贡？
13. 下一局谁先手？
14. 级牌怎么处理？
15. 逢人配规则是否和经典玩法相同？
16. 同花顺与炸弹大小如何比较？
17. 不同张数炸弹如何比较？
18. 一队玩家已经全部出完后，游戏是否立即结束？
19. 队友之间是否允许特殊配合规则？
20. 超时不出牌如何处理？

在这些规则没有确定以前，不建议开始实现完整 GameEngine。

---

# 13. GameRuleConfig

不要将所有规则直接写死。

建议：

```cpp
struct GameRuleConfig {
    int playerCount = 6;

    int deckCount;

    int teamCount = 2;

    int playersPerTeam = 3;

    bool enableTribute;

    bool enableReturnTribute;

    bool enableAntiTribute;

    bool enableWildCard;

    int turnTimeoutSeconds;
};
```

这样以后可以扩展：

```text
经典四人模式

六人娱乐模式

自定义规则模式
```

---

# 14. 游戏状态机

推荐使用明确的状态机。

```cpp
enum class GamePhase {
    WAITING,
    READY,
    DEALING,
    PLAYING,
    ROUND_END,
    SETTLEMENT,
    FINISHED
};
```

主要状态：

```text
WAITING
    │
    ▼
READY
    │
    ▼
DEALING
    │
    ▼
PLAYING
    │
    ▼
ROUND_END
    │
    ▼
SETTLEMENT
    │
    ▼
NEXT ROUND / FINISHED
```

不建议使用大量 Boolean：

```text
gameStarted
gameEnded
isDealing
isWaiting
isSettlement
```

否则后期容易出现非法状态组合。

---

# 15. GameState

服务器上的完整状态可设计：

```cpp
struct GameState {
    RoomId roomId;

    GamePhase phase;

    std::array<PlayerState, 6> players;

    PlayerId currentPlayer;

    int currentLevel;

    std::vector<CardId> lastPlayedCards;

    PlayerId lastPlayedPlayer;

    uint64_t turnId;

    uint64_t stateVersion;

    int round;
};
```

其中：

```text
turnId
```

表示当前回合。

```text
stateVersion
```

表示游戏状态版本。

这两个字段对于防止：

- 重复请求
- 消息乱序
- 网络重发
- 客户端旧状态覆盖新状态

非常重要。

---

# 16. 房间系统

## Room

```cpp
class Room {
private:
    RoomId roomId;

    std::array<Player, 6> players;

    RoomState roomState;

    GuandanEngine gameEngine;

public:
    bool join(Player player);

    void leave(PlayerId playerId);

    void ready(PlayerId playerId);

    void startGame();
};
```

---

## RoomManager

例如：

```cpp
std::unordered_map<
    RoomId,
    std::shared_ptr<Room>
> rooms;
```

结构：

```text
RoomManager

938271 -> Room
512634 -> Room
781234 -> Room
```

---

# 17. 创建房间流程

```text
Client

CREATE_ROOM
     │
     ▼
GameServer
     │
     ▼
RoomManager::createRoom()
     │
     ▼
生成唯一房间号
     │
     ▼
938271
     │
     ▼
返回客户端
```

其他玩家输入：

```text
938271
```

即可加入。

---

# 18. 房间内任务串行化

同一个房间中的游戏状态应该尽量采用：

```text
单房间串行执行
```

而不是多个工作线程同时修改 GameState。

例如：

```text
Network Thread 1 ─┐
Network Thread 2 ─┼─> Room Task Queue ─> GameEngine
Network Thread 3 ─┘
```

一个 Room 的任务：

```text
加入

准备

出牌

过牌

超时

断线

重连
```

按顺序处理。

这样能够大幅降低：

```text
mutex

数据竞争

死锁

复杂并发 Bug
```

的风险。

---

# 19. 网络协议

推荐统一协议格式：

```json
{
  "type": "play_cards",
  "request_id": 10086,
  "room_id": "938271",
  "state_version": 28,
  "data": {}
}
```

建议每条消息至少包含：

```text
protocol_version

type

request_id

room_id

data
```

游戏操作最好额外包含：

```text
turn_id

state_version
```

---

# 20. 常见网络消息

## 创建房间

```json
{
  "type": "create_room",
  "request_id": 1
}
```

---

## 加入房间

```json
{
  "type": "join_room",
  "request_id": 2,
  "room_id": "938271"
}
```

---

## 玩家准备

```json
{
  "type": "ready",
  "request_id": 3,
  "room_id": "938271"
}
```

---

## 出牌

```json
{
  "type": "play_cards",
  "request_id": 4,
  "room_id": "938271",
  "turn_id": 37,
  "cards": [10, 23, 41]
}
```

---

## 过牌

```json
{
  "type": "pass",
  "request_id": 5,
  "room_id": "938271",
  "turn_id": 37
}
```

---

# 21. 服务端事件广播

出牌成功：

```json
{
  "type": "player_played",
  "room_id": "938271",
  "player_id": 10001,
  "cards": [10, 23, 41],
  "next_player": 10002,
  "state_version": 29
}
```

---

# 22. JSON 与 Protobuf

第一版推荐：

```text
JSON
```

原因：

- 开发快
- 可读性高
- 抓包方便
- 日志方便
- 调试方便

以后如果确实需要优化：

```text
JSON
  ↓
Protobuf
```

协议层应该做抽象，避免业务代码直接依赖 JSON。

---

# 23. 发牌

洗牌必须由服务器完成。

```text
Server

生成牌堆
   ↓
随机洗牌
   ↓
按照规则发牌
   ↓
保存真实手牌
   ↓
向各客户端发送自己的手牌
```

必须保证：

```text
玩家 A

只知道 A 的手牌
```

玩家 A 不应该通过网络协议获得 B、C、D、E、F 的真实手牌。

客户端只需要知道：

```text
B：剩余 18 张

C：剩余 21 张
```

---

# 24. 随机数设计

洗牌不要直接使用简单的：

```cpp
rand()
```

推荐使用：

```cpp
std::random_device
+
std::mt19937 / std::mt19937_64
```

如果未来涉及正式竞技、公平性证明或เงินจริง经济系统，还需要进一步设计可审计的随机机制。

---

# 25. 断线重连

棋牌游戏必须支持断线重连。

例如：

```text
P3

WebSocket 断开
      │
      ▼
Session 被释放
      │
      ▼
Player 仍然存在
      │
      ▼
Room 仍然存在
      │
      ▼
GameState 继续保留
```

玩家状态：

```text
ONLINE
   ↓
OFFLINE
```

重新连接：

```text
Client

登录 Token
   │
   ▼
服务器认证
   │
   ▼
找到 user_id
   │
   ▼
发现玩家正在 Room 938271
   │
   ▼
绑定新的 WebSocket Session
   │
   ▼
发送完整 GameSnapshot
```

---

# 26. GameSnapshot

重新连接时不要尝试逐条补所有历史消息。

推荐直接发送当前完整快照。

```json
{
  "type": "game_snapshot",

  "room_id": "938271",

  "state_version": 105,

  "phase": "PLAYING",

  "players": [],

  "my_cards": [],

  "last_play": {},

  "current_player": 10003,

  "turn_id": 53
}
```

客户端直接根据 GameSnapshot 重建 UI。

---

# 27. 心跳

客户端可以周期性发送：

```text
PING
```

服务器：

```text
PONG
```

例如：

```text
每 15 秒发送一次心跳
```

如果多次无法收到客户端消息：

```text
ONLINE
   ↓
OFFLINE
```

但不要立即删除玩家的 Room 和 GameState。

---

# 28. 操作超时

当前方案必须补充操作超时机制。

例如：

```text
玩家轮到出牌
    │
    ▼
服务器启动 30 秒 TurnTimer
    │
    ├── 30 秒内操作
    │       ↓
    │    正常处理
    │
    └── 超时
            ↓
       自动 Pass / 托管
```

具体行为需要由玩法规则确定。

不要由客户端计时决定超时。

客户端倒计时只负责显示。

真正的 Timer 必须在服务器。

---

# 29. PostgreSQL

PostgreSQL 保存长期数据。

## users

```text
id

username

password_hash

nickname

avatar

created_at
```

## games

```text
id

room_id

started_at

finished_at

winner_team

rule_version
```

## game_players

```text
game_id

user_id

team

finish_rank

score
```

## game_actions

可选。

```text
game_id

sequence

player_id

action_type

payload

created_at
```

---

# 30. Redis

Redis 第一版不是必须。

可以在第二阶段用于：

```text
登录 Session

Token

在线状态

限流

临时房间索引

分布式服务器情况下的房间路由
```

如果当前只有一台服务器：

```text
C++ 内存 Room
+
PostgreSQL
```

已经可以完成 MVP。

---

# 31. Linux 部署

推荐：

```text
Linux Server

├── Nginx
│
├── GameServer
│
├── PostgreSQL
│
├── Redis
│
└── Log
```

客户端：

```text
Android / iOS
      │
      │
      ▼
    Internet
      │
      ▼
     443
      │
      ▼
    Nginx
      │
      ▼
GameServer
```

正式发布推荐：

```text
https://api.example.com

wss://game.example.com/ws
```

而不是直接让客户端访问：

```text
IP:9092
```

---

# 32. 日志

至少应该有：

```text
server.log

room.log

game.log

error.log
```

关键日志包括：

```text
用户登录

创建房间

进入房间

退出房间

掉线

重连

游戏开始

玩家出牌

非法操作

游戏结束

异常
```

建议日志中携带：

```text
user_id

room_id

game_id

request_id

turn_id

state_version
```

方便线上排查问题。

---

# 33. 自动化测试

GameEngine 应该有独立测试。

例如：

```text
test_single

test_pair

test_triple

test_straight

test_bomb

test_wild_card

test_compare_card

test_turn

test_pass

test_game_finish

test_settlement

test_tribute
```

特别需要测试：

```text
边界情况

非法牌型

逢人配

多种炸弹

玩家出完牌

连续多人 Pass

断线

超时

重复请求

消息乱序
```

游戏规则测试的重要程度甚至高于 UI 测试。

---

# 34. MVP 开发范围

第一版只实现：

```text
登录
 ↓
创建房间
 ↓
六名玩家加入
 ↓
准备
 ↓
开始游戏
 ↓
发牌
 ↓
出牌
 ↓
过牌
 ↓
牌型判定
 ↓
回合推进
 ↓
游戏结束
 ↓
结算
 ↓
再来一局
```

暂时不要优先开发：

- 排行榜
- 好友
- 公会
- 商城
- 皮肤
- 金币
- 充值
- 语音
- 观战
- 匹配系统
- 赛事系统

---

# 35. 推荐开发顺序

## 第一阶段：游戏规则

1. 明确六人掼蛋规则
2. Card
3. Deck
4. Hand
5. CardAnalyzer
6. RuleEngine
7. TurnManager
8. Settlement
9. GameState
10. 自动化测试

---

## 第二阶段：服务器

1. WebSocket Server
2. Session
3. Protocol
4. Room
5. RoomManager
6. Room Task Queue
7. GameEngine 接入
8. 心跳
9. 超时管理
10. 断线重连

---

## 第三阶段：客户端

1. 登录页面
2. 大厅
3. 创建房间
4. 加入房间
5. 房间 UI
6. 六人牌桌
7. 手牌显示
8. 选牌
9. 出牌动画
10. 游戏结算

---

## 第四阶段：数据和部署

1. PostgreSQL
2. 用户系统
3. 游戏历史
4. Nginx
5. HTTPS / WSS
6. 日志
7. 监控
8. Docker
9. 自动化部署

---

## 第五阶段：发布

1. Android 真机测试
2. iOS 真机测试
3. 弱网测试
4. 多设备测试
5. 压力测试
6. Android 发布
7. iOS 发布

---

# 36. 原方案存在的不足

下面是对最初方案的进一步分析。

---

## 36.1 最大不足：六人掼蛋规则没有定义

这是当前最严重的问题。

经典掼蛋主要围绕：

```text
4个人

2个队伍

每队2个人
```

设计。

现在改成：

```text
6个人

2队

每队3个人
```

很多核心规则不能自然直接继承。

如果规则没有提前确定，后面：

```text
GameEngine

牌型

结算

进贡

升级

UI

协议
```

都会反复修改。

### 改进方案

第一项工作不是写客户端，也不是搭服务器。

而是先写：

```text
《六人掼蛋游戏规则说明书 V1.0》
```

并将所有边界情况明确。

---

# 36.2 服务端使用 C++ 的开发成本较高

C++ 很适合高性能网络服务器。

但是掼蛋并不是高频网络游戏。

第一版使用：

```text
C++
Boost.Asio
Boost.Beast
PostgreSQL
Redis
Nginx
Flutter
```

会涉及大量技术栈。

如果开发者数量较少，开发成本会明显增加。

### 方案 A

如果目标是学习 C++ 网络开发：

```text
保留 C++
```

这是很好的工程实践项目。

### 方案 B

如果目标是最快上线：

```text
Go
Node.js
Java / Spring Boot
```

开发房间和 WebSocket 业务会更快。

因此技术选型应该根据目标决定：

```text
学习 / C++工程经验
      ↓
     C++

快速产品上线
      ↓
 Go / Java / Node.js
```

---

# 36.3 Redis 第一版可能属于过度设计

只有一台 Linux 游戏服务器时：

```text
Room
GameState
Session
```

完全可以先存内存。

同时使用：

```text
PostgreSQL
+
Redis
```

会增加：

- 部署成本
- 数据一致性问题
- 调试成本
- 运维成本

### 推荐

MVP：

```text
C++ Server
+
PostgreSQL
```

第二阶段再引入 Redis。

---

# 36.4 原方案缺少 request_id / state_version / turn_id

仅仅使用 WebSocket 并不能保证业务层不会出现：

```text
重复点击

客户端重试

消息乱序

旧请求到达

断线恢复后的旧消息
```

因此游戏请求应该带：

```text
request_id
turn_id
state_version
```

服务器必须实现幂等与过期请求检查。

这是实时游戏服务器非常重要的一部分。

---

# 36.5 原方案断线重连设计还不够完整

只定义：

```text
重新连接后发送 Snapshot
```

还不够。

需要明确：

```text
用户多久可以重连？

断线玩家轮到出牌怎么办？

掉线 1 分钟后怎么办？

六个人中多人同时掉线怎么办？

房主掉线房间是否继续？

游戏结束后离线玩家如何收到结果？
```

因此需要：

```text
ReconnectPolicy
+
OfflinePolicy
+
AutoPlayPolicy
```

---

# 36.6 缺少服务器计时器设计

棋牌游戏虽然不像 FPS 高频 Tick，但依然需要大量 Timer：

```text
出牌倒计时

心跳检测

断线重连窗口

房间空闲销毁

准备超时

托管
```

因此服务端应该增加：

```text
TimerManager
```

或者直接利用 Boost.Asio：

```text
steady_timer
```

实现异步定时任务。

---

# 36.7 缺少房间生命周期设计

Room 不应该永远存在。

建议：

```text
CREATED
   ↓
WAITING
   ↓
PLAYING
   ↓
SETTLEMENT
   ↓
FINISHED
   ↓
DESTROYED
```

例如：

```text
房间中已经没有玩家
      ↓
5分钟
      ↓
销毁 Room
```

需要明确：

- 房主退出
- 游戏未开始时玩家退出
- 游戏开始后退出
- 游戏结束后所有人退出
- 房间长期空闲

---

# 36.8 缺少协议版本控制

移动 App 发布以后，不是所有用户都会同时升级。

可能存在：

```text
客户端 1.0

客户端 1.1

服务器 2.0
```

所以协议最好包含：

```json
{
  "protocol_version": 1
}
```

服务器可以拒绝不兼容客户端。

---

# 36.9 缺少错误码体系

服务器不能仅返回：

```text
失败
```

应该有：

```cpp
enum class ErrorCode {
    OK,

    INVALID_TOKEN,

    ROOM_NOT_FOUND,

    ROOM_FULL,

    ALREADY_IN_ROOM,

    NOT_YOUR_TURN,

    INVALID_CARDS,

    CANNOT_BEAT,

    INVALID_STATE,

    REQUEST_EXPIRED,

    DUPLICATE_REQUEST
};
```

客户端才能正确展示不同错误。

---

# 36.10 缺少安全设计

至少应该考虑：

```text
HTTPS / WSS

Token 验证

密码 Hash

登录限流

接口限流

房间号爆破

恶意创建大量房间

WebSocket 消息大小限制

非法 JSON

畸形数据包

频繁出牌请求

客户端修改手牌
```

所有输入都必须视为不可信。

---

# 36.11 缺少防作弊设计

虽然服务器权威已经解决一部分问题，但仍然要防止：

```text
自动脚本

机器人批量操作

偷窥协议数据

恶意重复请求

速度异常

多账号协作
```

至少保证：

```text
其他玩家的手牌绝不发送给客户端

洗牌只在服务器

规则只在服务器判定

结算只由服务器计算
```

---

# 36.12 缺少游戏回放和问题追踪

棋牌游戏非常适合记录操作日志。

不一定每次保存完整 GameState。

可以保存：

```text
GameStart

ShuffleSeed / DeckOrder（视安全策略）

Deal

Play

Pass

Timeout

Reconnect

GameEnd
```

以后发生：

```text
玩家反馈牌不对

结算不对

服务器 Bug
```

可以重放整个游戏过程。

---

# 36.13 缺少监控系统

只有日志还不够。

后期推荐监控：

```text
在线连接数

在线房间数

当前游戏数

WebSocket 断线率

重连成功率

接口延迟

CPU

内存

数据库连接数

异常数量
```

可以后期使用：

```text
Prometheus
+
Grafana
```

---

# 36.14 缺少弱网处理

移动网络经常会：

```text
Wi-Fi → 5G

5G → Wi-Fi

进入电梯

锁屏

App 切后台

网络短暂断开
```

Flutter 客户端需要：

```text
自动重连

指数退避

Token 恢复

Snapshot 恢复

UI 网络状态提示
```

---

# 36.15 缺少 App 生命周期处理

手机应用不是桌面程序。

必须考虑：

```text
App 切到后台

手机锁屏

系统冻结 App

App 被系统杀死

App 再次启动
```

重新打开后：

```text
登录 Token
   ↓
查询当前游戏状态
   ↓
若仍在游戏
   ↓
恢复 Room
   ↓
加载 Snapshot
```

---

# 36.16 缺少状态快照的数据隐藏设计

服务器完整 GameState 中拥有：

```text
所有人的手牌
```

但不能直接将完整 GameState 广播。

应该存在：

```cpp
GameState
```

和：

```cpp
PlayerView
```

例如：

```cpp
PlayerView buildViewFor(PlayerId viewer);
```

对于 P1：

```text
自己的手牌
    ↓
完整显示

P2~P6手牌
    ↓
只显示数量
```

这是避免泄露手牌的重要设计。

---

# 36.17 缺少压力测试策略

棋牌游戏单局流量很小。

压力主要来自：

```text
大量 WebSocket 长连接

大量房间

心跳

房间状态

数据库连接
```

后期需要测试：

```text
100连接

1000连接

5000连接

10000连接
```

观察：

```text
CPU

内存

P99延迟

断线率
```

不要只测试单局是否能玩。

---

# 36.18 单服务器存在单点故障

MVP 使用一台服务器没问题。

但如果服务器宕机：

```text
所有正在进行的游戏
      ↓
同时中断
```

第一版可以接受。

后期则需要：

```text
Gateway

多个 GameServer

Redis / 服务发现

Room 路由

GameState 持久化 / 快照
```

因此当前架构适合：

```text
MVP
+
早期用户
```

不适合直接作为大规模商业架构。

---

# 36.19 缺少服务端崩溃恢复

断线重连只解决：

```text
客户端掉线
```

没有解决：

```text
GameServer 崩溃
```

如果 Room 只存在内存：

```text
GameServer 重启
   ↓
所有进行中游戏丢失
```

MVP 可以暂时接受。

如果需要提高可靠性，可以周期保存：

```text
GameSnapshot
```

或者使用：

```text
Event Sourcing
```

记录游戏事件，然后恢复状态。

---

# 36.20 缺少版本化的游戏规则

未来规则可能修改。

例如：

```text
V1 炸弹大小规则

V2 炸弹大小规则
```

已经开始的游戏不能因为服务器升级突然改变规则。

所以 Game 记录中建议保存：

```text
rule_version
```

例如：

```text
six_player_guandan_v1
```

---

# 37. 改进后的推荐 MVP 架构

对于当前阶段，最推荐：

```text
                Flutter

       Android          iOS
           \             /
            \           /
               HTTPS
                WSS
                 │
                 ▼
               Nginx
                 │
                 ▼
        C++ Game Server
                 │
     ┌───────────┼───────────┐
     │           │           │
  Session     RoomManager  GameEngine
                 │
                 │
             PostgreSQL
```

暂时：

```text
不使用 Redis

不做微服务

不做 UDP

不做分布式

不做复杂商城

不做排行榜

不做匹配
```

先把：

```text
六人规则
+
GameEngine
+
Room
+
WebSocket
+
断线重连
```

做稳定。

---

# 38. 推荐最终核心模块

```text
GameServer
│
├── Network
│
├── Authentication
│
├── SessionManager
│
├── Protocol
│
├── RoomManager
│      │
│      └── Room
│           │
│           ├── RoomTaskQueue
│           └── GameEngine
│                  │
│                  ├── Deck
│                  ├── Hand
│                  ├── CardAnalyzer
│                  ├── RuleEngine
│                  ├── TurnManager
│                  ├── GameState
│                  └── Settlement
│
├── TimerManager
│
├── Persistence
│
├── Logger
│
└── Metrics
```

---

# 39. 当前最应该做的五件事情

按照优先级：

## 第一件

完成：

```text
《六人掼蛋规则说明书 V1.0》
```

这是最高优先级。

---

## 第二件

完成：

```text
GameEngine
```

并且确保：

```text
GameEngine 完全不依赖 Flutter

GameEngine 完全不依赖 WebSocket

GameEngine 完全不依赖 PostgreSQL
```

---

## 第三件

为 GameEngine 建立自动化测试。

至少把所有：

```text
牌型

大小比较

轮次

结束条件

结算
```

覆盖。

---

## 第四件

实现：

```text
C++ WebSocket

Session

Room

RoomManager
```

先让六个测试客户端能够：

```text
创建房间
+
加入房间
+
收到广播
```

---

## 第五件

最后再开始制作正式 Flutter 游戏桌面 UI。

---

# 40. 结论

这款软件适合采用：

```text
Flutter
+
C++ WebSocket Server
+
PostgreSQL
+
Linux
```

的技术架构。

最重要的设计原则是：

```text
1. 服务器权威

2. GameEngine 与网络层解耦

3. 单个房间游戏逻辑串行执行

4. 所有玩家只能看到自己有权限看到的状态

5. 使用 request_id / turn_id / state_version 保证消息一致性

6. 使用 Snapshot 支持断线重连

7. 游戏规则先于客户端开发

8. 第一版保持单体架构，不做过度设计
```

当前项目最大的风险并不是服务器性能，而是：

```text
六人规则定义不完整
+
游戏状态一致性
+
边界情况
+
断线/超时处理
```

因此第一阶段最值得投入时间的不是 UI，而是：

```text
规则文档
+
GameEngine
+
自动化测试
```

这三部分稳定后，再继续网络层和移动客户端开发，可以显著降低整个项目后期返工成本。
# 41. 项目完整目录结构要求

为了保证客户端、服务端、协议文档、部署配置和项目文档之间职责清晰，建议整个项目采用如下目录结构。

```text
gd/
│
├── client/                         # Flutter 手机客户端
│   │
│   ├── android/                    # Android 平台相关代码
│   ├── ios/                        # iOS 平台相关代码
│   │
│   ├── assets/                     # 图片、音效、字体等资源
│   │   ├── images/
│   │   │   ├── cards/              # 扑克牌图片
│   │   │   ├── avatars/
│   │   │   └── backgrounds/
│   │   │
│   │   └── sounds/
│   │       ├── play_card.mp3
│   │       ├── pass.mp3
│   │       └── win.mp3
│   │
│   ├── lib/
│   │   │
│   │   ├── main.dart               # Flutter 程序入口
│   │   ├── app.dart                # App 总体配置
│   │   ├── router.dart             # 页面路由
│   │   │
│   │   ├── pages/                  # 页面
│   │   │   ├── login/
│   │   │   │   └── login_page.dart
│   │   │   │
│   │   │   ├── lobby/
│   │   │   │   └── lobby_page.dart
│   │   │   │
│   │   │   ├── room/
│   │   │   │   └── room_page.dart
│   │   │   │
│   │   │   ├── game/
│   │   │   │   └── game_page.dart
│   │   │   │
│   │   │   └── result/
│   │   │       └── result_page.dart
│   │   │
│   │   ├── widgets/                # 通用 UI 组件
│   │   │   ├── poker_card.dart
│   │   │   ├── player_widget.dart
│   │   │   ├── game_table.dart
│   │   │   ├── hand_cards.dart
│   │   │   └── countdown.dart
│   │   │
│   │   ├── models/                 # 客户端数据模型
│   │   │   ├── user.dart
│   │   │   ├── player.dart
│   │   │   ├── room.dart
│   │   │   ├── card.dart
│   │   │   └── game_state.dart
│   │   │
│   │   ├── network/                # 网络通信
│   │   │   ├── http_client.dart
│   │   │   ├── websocket_client.dart
│   │   │   ├── reconnect_manager.dart
│   │   │   └── protocol.dart
│   │   │
│   │   ├── controller/             # 页面/业务控制
│   │   │   ├── auth_controller.dart
│   │   │   ├── room_controller.dart
│   │   │   └── game_controller.dart
│   │   │
│   │   ├── state/                  # 状态管理
│   │   │   ├── user_state.dart
│   │   │   ├── room_state.dart
│   │   │   └── game_state_provider.dart
│   │   │
│   │   └── utils/
│   │       ├── constants.dart
│   │       ├── logger.dart
│   │       └── card_utils.dart
│   │
│   ├── test/                       # Flutter 单元测试
│   │
│   └── pubspec.yaml
│
│
├── server/                         # C++ Linux 游戏服务器
│   │
│   ├── CMakeLists.txt
│   │
│   ├── config/
│   │   ├── server.json
│   │   └── database.json
│   │
│   ├── include/
│   │   │
│   │   ├── network/
│   │   ├── auth/
│   │   ├── room/
│   │   ├── game/
│   │   ├── protocol/
│   │   ├── database/
│   │   └── utils/
│   │
│   ├── src/
│   │   │
│   │   ├── main.cpp                # 服务端入口
│   │   │
│   │   ├── network/                # 网络层
│   │   │   ├── websocket_server.cpp
│   │   │   ├── websocket_session.cpp
│   │   │   ├── connection_manager.cpp
│   │   │   └── message_dispatcher.cpp
│   │   │
│   │   ├── auth/                   # 登录认证
│   │   │   ├── auth_service.cpp
│   │   │   └── token_manager.cpp
│   │   │
│   │   ├── user/
│   │   │   ├── user.cpp
│   │   │   └── user_manager.cpp
│   │   │
│   │   ├── room/                   # 房间系统
│   │   │   ├── room.cpp
│   │   │   ├── room_manager.cpp
│   │   │   └── room_task_queue.cpp
│   │   │
│   │   ├── game/                   # 掼蛋核心规则
│   │   │   ├── card.cpp
│   │   │   ├── deck.cpp
│   │   │   ├── hand.cpp
│   │   │   ├── card_pattern.cpp
│   │   │   ├── card_analyzer.cpp
│   │   │   ├── rule_engine.cpp
│   │   │   ├── turn_manager.cpp
│   │   │   ├── game_state.cpp
│   │   │   ├── game_engine.cpp
│   │   │   └── settlement.cpp
│   │   │
│   │   ├── protocol/               # 客户端/服务端协议
│   │   │   ├── message.cpp
│   │   │   ├── message_type.cpp
│   │   │   ├── error_code.cpp
│   │   │   └── protocol_codec.cpp
│   │   │
│   │   ├── database/
│   │   │   ├── postgresql.cpp
│   │   │   └── redis.cpp
│   │   │
│   │   ├── timer/
│   │   │   └── timer_manager.cpp
│   │   │
│   │   └── utils/
│   │       ├── logger.cpp
│   │       ├── random.cpp
│   │       ├── uuid.cpp
│   │       └── config.cpp
│   │
│   ├── tests/                      # C++ 自动化测试
│   │   ├── test_card.cpp
│   │   ├── test_card_analyzer.cpp
│   │   ├── test_rule_engine.cpp
│   │   ├── test_game_engine.cpp
│   │   ├── test_settlement.cpp
│   │   └── test_room.cpp
│   │
│   ├── scripts/
│   │   ├── build.sh
│   │   ├── start.sh
│   │   └── stop.sh
│   │
│   └── logs/
│
│
├── protocol/                       # 客户端、服务器共享的协议文档
│   ├── protocol.md
│   ├── message_types.md
│   └── error_codes.md
│
├── docs/                           # 项目设计文档
│   ├── architecture.md
│   ├── game_rules.md
│   ├── database.md
│   ├── reconnect.md
│   └── deployment.md
│
├── deploy/                         # Linux 部署配置
│   ├── nginx/
│   │   └── guandan.conf
│   ├── docker/
│   │   └── Dockerfile
│   └── docker-compose.yml
│
├── README.md
│
└── .gitignore
```

---

## 41.1 顶层目录职责

### `client/`

用于存放 Flutter 手机客户端代码。

最终负责构建：

```text
Android -> APK / AAB

iOS -> IPA
```

客户端主要负责：

- 登录页面
- 房间页面
- 六人游戏桌面
- 手牌显示
- 选牌与出牌交互
- 动画和音效
- WebSocket 网络连接
- 接收服务端状态
- 弱网和断线重连
- 游戏状态 UI 展示

客户端不得自行决定最终游戏结果。

---

### `server/`

用于存放部署到 Linux 云服务器上的 C++ 游戏服务器代码。

主要负责：

- 网络连接
- 用户认证
- Session 管理
- 房间管理
- 游戏规则
- 游戏状态
- 出牌判定
- 游戏结算
- 心跳
- 超时
- 断线重连
- 数据库存储

服务端是整个游戏的权威状态来源。

---

### `protocol/`

用于维护客户端与服务端共同遵守的通信协议。

建议至少维护：

```text
protocol.md

message_types.md

error_codes.md
```

需要明确：

- 请求格式
- 响应格式
- 广播格式
- 消息类型
- 错误码
- protocol_version
- request_id
- turn_id
- state_version

不要仅依靠代码隐式定义通信协议。

---

### `docs/`

用于维护整个项目的设计文档。

其中：

```text
architecture.md
```

描述整体技术架构。

```text
game_rules.md
```

描述六人掼蛋完整规则，是 GameEngine 开发的重要依据。

```text
database.md
```

描述数据库表结构。

```text
reconnect.md
```

描述断线重连、掉线、托管策略。

```text
deployment.md
```

描述 Linux 服务器部署方式。

---

### `deploy/`

用于保存服务器上线所需的配置。

包括：

- Nginx 配置
- Dockerfile
- docker-compose
- 后续 CI/CD 配置

不要将正式部署脚本散落到业务代码目录。

---

# 42. 客户端目录职责说明

## `pages/`

负责完整页面。

例如：

```text
login_page.dart

lobby_page.dart

room_page.dart

game_page.dart

result_page.dart
```

页面主要负责组合 UI，不建议在页面内部直接写大量网络或游戏业务逻辑。

---

## `widgets/`

存放可复用 UI 组件。

例如：

```text
PokerCard

PlayerWidget

GameTable

HandCards

Countdown
```

后期牌桌 UI 会大量依赖这些组件。

---

## `models/`

客户端本地的数据结构。

例如：

```text
User

Player

Room

Card

GameState
```

注意客户端 `GameState` 是客户端可见状态，并不是服务器内部完整 `GameState`。

---

## `network/`

客户端所有网络代码统一放在这里。

调用关系建议：

```text
Page
   ↓
Controller
   ↓
WebSocketClient
   ↓
Server
```

不要在每个页面中直接创建 WebSocket。

---

## `controller/`

负责业务控制。

例如：

```text
GamePage
   ↓
GameController
   ↓
WebSocketClient
```

玩家点击“出牌”后：

```text
GamePage
    ↓
GameController.playCards()
    ↓
WebSocketClient.send()
    ↓
C++ Server
```

---

## `state/`

负责 Flutter 全局状态。

例如：

```text
当前用户

当前房间

当前游戏状态

WebSocket 连接状态
```

可以使用：

```text
Riverpod
```

或者：

```text
Provider
```

第一版二选一即可，不要同时使用多个状态管理框架。

---

# 43. 服务端目录职责说明

## `network/`

专门负责：

```text
TCP

HTTP Upgrade

WebSocket

连接生命周期

消息读取

消息发送
```

不负责掼蛋规则。

---

## `message_dispatcher`

负责：

```text
收到 JSON
    ↓
解析 type
    ↓
找到对应 Handler
```

例如：

```text
CREATE_ROOM
      ↓
RoomService

PLAY_CARDS
      ↓
GameService
```

---

## `auth/`

负责：

- 登录
- Token
- 身份认证
- WebSocket 鉴权

---

## `room/`

负责：

```text
Room

RoomManager

RoomTaskQueue
```

关系：

```text
RoomManager
   │
   ├── Room 100001
   ├── Room 100002
   └── Room 100003
```

每个正在进行游戏的 Room 拥有自己的：

```text
GameEngine
```

---

## `game/`

这是整个 C++ 服务端最重要的目录。

主要调用关系：

```text
GameEngine
     │
     ├── GameState
     │
     ├── CardAnalyzer
     │
     ├── RuleEngine
     │
     ├── TurnManager
     │
     └── Settlement
```

其中：

### `card`

定义一张实体牌。

### `deck`

负责：

- 创建牌堆
- 洗牌
- 发牌

### `hand`

管理玩家手牌。

### `card_pattern`

描述经过识别后的牌型。

### `card_analyzer`

负责牌型识别。

### `rule_engine`

负责牌型比较和规则合法性判断。

### `turn_manager`

负责：

- 当前玩家
- 下一玩家
- Pass
- 新一轮出牌权

### `game_state`

保存当前完整服务器游戏状态。

### `game_engine`

整个单局游戏的核心控制器。

### `settlement`

负责：

- 排名
- 胜负
- 分数
- 升级
- 进贡相关结果

---

# 44. 客户端与服务端调用链路

整个项目最核心的调用链：

```text
┌──────────────────────────────┐
│        Flutter Client        │
│                              │
│ GamePage                     │
│    ↓                         │
│ GameController               │
│    ↓                         │
│ WebSocketClient              │
└──────────────┬───────────────┘
               │
            WebSocket
               │
┌──────────────▼───────────────┐
│        C++ GameServer        │
│                              │
│ WebSocketSession             │
│       ↓                      │
│ MessageDispatcher            │
│       ↓                      │
│ RoomManager                  │
│       ↓                      │
│ Room                         │
│       ↓                      │
│ GameEngine                   │
│       ↓                      │
│ RuleEngine / CardAnalyzer    │
│       ↓                      │
│ GameState                    │
└──────────────┬───────────────┘
               │
         PostgreSQL
```

例如玩家点击出牌：

```text
Flutter GamePage
       ↓
GameController
       ↓
WebSocketClient
       ↓
PLAY_CARDS
       ↓
C++ WebSocketSession
       ↓
MessageDispatcher
       ↓
Room
       ↓
GameEngine
       ↓
CardAnalyzer
       ↓
RuleEngine
       ↓
更新 GameState
       ↓
广播结果
       ↓
Flutter 更新 UI
```

---

# 45. 实际开发时建议先使用精简目录

完整目录是最终目标，但项目刚开始时不建议一次性创建所有空文件。

第一阶段可以先使用：

```text
Guandan/
│
├── server/
│   ├── CMakeLists.txt
│   │
│   ├── src/
│   │   ├── main.cpp
│   │   │
│   │   ├── game/
│   │   │   ├── card.h
│   │   │   ├── card.cpp
│   │   │   ├── deck.h
│   │   │   ├── deck.cpp
│   │   │   ├── card_analyzer.h
│   │   │   ├── card_analyzer.cpp
│   │   │   ├── rule_engine.h
│   │   │   ├── rule_engine.cpp
│   │   │   ├── game_engine.h
│   │   │   └── game_engine.cpp
│   │   │
│   │   ├── room/
│   │   │   ├── room.h
│   │   │   └── room.cpp
│   │   │
│   │   └── network/
│   │       ├── server.h
│   │       └── server.cpp
│   │
│   └── tests/
│       └── test_game.cpp
│
└── client/
    └── Flutter 项目
```

推荐开发顺序：

```text
Card
 ↓
Deck
 ↓
CardAnalyzer
 ↓
RuleEngine
 ↓
GameEngine
 ↓
自动化测试
 ↓
Room
 ↓
WebSocket Server
 ↓
六个测试客户端
 ↓
Flutter Client
```

等核心功能逐渐完成后，再逐步演进到前面的完整目录结构。

---

# 46. 目录结构设计原则

项目代码需要遵守以下原则：

1. **客户端与服务端完全分离。**
2. **GameEngine 不依赖 Flutter。**
3. **GameEngine 不依赖 WebSocket。**
4. **GameEngine 尽量不直接依赖数据库。**
5. **网络层不能包含掼蛋牌型规则。**
6. **UI 层不能决定游戏结果。**
7. **客户端和服务端必须共同遵守 protocol 文档。**
8. **服务器内部完整 GameState 与客户端 PlayerView 必须区分。**
9. **一个 Room 对应一个独立游戏上下文。**
10. **测试代码独立存放，核心规则必须可脱离网络测试。**
11. **部署配置与业务源码分离。**
12. **项目文档、游戏规则和通信协议都应进行版本管理。**

这套目录结构作为项目最终目标，实际开发时应从最小可运行结构逐步扩展，避免项目初期产生大量没有实际用途的空模块。
